export interface RaidGroupMember{
    raidGroupId: number;
}

export interface RaidGroupMembers{
    id: number;
    username: string;
}

export interface RaidMemberDTO{
    raidGroupId: number;
    userId: number;
}